{
  "name": "lightweight-portfolio-site",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^7.13.2",
    "vite": "^8.0.2"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.34.0",
    "wrangler": "^4.86.0"
  }
}
